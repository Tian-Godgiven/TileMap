```
swal.fire({
            title: "",
            html: ''
        }).then((result) => {
        	//确认
            if (result.isConfirmed) {
            } 
            //取消
            else {

            }
        });
```

